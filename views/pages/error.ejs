<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="<%= site.metadata.charset %>">
    <meta name="viewport" content="<%= site.metadata.viewport %>">
    <meta name="description" content="<%= site.metadata.description %>">
    <meta name="keywords" content="<%= site.metadata.keywords %>">
    <meta name="author" content="<%= site.metadata.author %>">
    <meta name="robots" content="<%= error.status === 404 ? 'noindex' : 'index, follow' %>">
    <title><%= error.status %> - <%= error.message %> | <%= site.title %></title>
    <link rel="stylesheet" type="text/css" href="/assets/css/style.css">
    <link rel="stylesheet" type="text/css" href="/assets/css/error.css">
</head>
<body>
<%- include('../partials/header') %>
<main class="error-container">
    <h1 class="error-code"><%= error.status %></h1>
    <p class="error-message"><%= error.message %></p>
    <p class="error-info">
        <% if (error.status === 404) { %>
            Sorry, the page you're looking for cannot be found. It may have been moved or deleted.
        <% } else if (error.status === 500) { %>
            An unexpected error occurred on our server. Our team has been notified and is working to resolve it.
        <% } else { %>
            An error occurred. Please try again later or contact support if the issue persists.
        <% } %>
    </p>
    <a href="/" class="back-home btn">Return to Home</a>
</main>
<%- include('../partials/footer') %>
<script type="application/javascript" src="/assets/js/main.js"></script>
</body>
</html>
